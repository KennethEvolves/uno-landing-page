import { SeoComponent } from '../shared'

export interface DegreeSeo {
  id: number
  documentId: string
  seo: SeoComponent
}

export interface Degree {
  id: number
  documentId: string
  name: string
}
